# GradSharp frontend configuration

# Leave blank for local-first app behaviour with graceful backend-not-connected AI messages.
# Set to your backend URL when testing the mock AI backend locally or on VPS.
# Example local backend:
# VITE_GRADSHARP_API_URL=http://localhost:8787
VITE_GRADSHARP_API_URL=
